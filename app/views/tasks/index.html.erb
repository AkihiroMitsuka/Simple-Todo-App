<table>
  <thead>
  <tr>
    <th>Tasks</th>
  </tr>
  </thead>
  <tbody>
  <% @task.each do |task| %>
    <tr>
      <td><%= task.title %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<%= link_to 'Create', new_task_path %>